import React from "react";
import styled from "styled-components";

const CustomerForm = () => {
  return <FormDiv>This is for the customer's information form</FormDiv>;
};

const FormDiv = styled.div``;

export default CustomerForm;
